var liczby = [1,2,3,8,15];
var liczby2 = [1,2,3,8,15];

//function pokazTablice( liczby ){
//    console.log( );
}

function pokazTablice( arr == liczby ){
    console.log( arr );
pokazTablice ( liczby );
//    lub 
    pokazTablice ( liczby2 );

    
    pokazTablice( ['a', 'b', 'c',]);

    
//    nazwa parametru ( arr, zmienna1 ) jest dowolna (z kilkoma ograniczeniami)
    //nie należy zapisywać parametrow jak zmiennych
